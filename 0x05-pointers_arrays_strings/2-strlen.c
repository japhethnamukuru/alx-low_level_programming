#include "main.h"
#include <string.h>

/**
 * _strlen - Returns length of a string
 *
 * *@s: Pointer to string
 *
 * Return: leghth of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
