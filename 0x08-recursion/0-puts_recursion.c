#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - prints a string follwed by a new line
 *
 * @s: pointer to the string array to print
 *
 * Return: void
 */

void _puts_recursion(char *s)
{
	puts(s);
}
