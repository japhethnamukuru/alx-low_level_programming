#include "main.h"
#include <stdlib.h>

/**
 * _abs - functions that returns the absolute value of a value
 *
 * @a: Returns an absolute value
 *
 * Return: return absolute value
 */
int _abs(int a)
{
	int i = abs(a);

	return (i);
}
